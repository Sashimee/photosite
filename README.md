# photosite

A photography website. Stack and setup instructions will be added once the project is scaffolded.

## Branching

`feature -> dev -> main`. Branch off `dev` (`feat/<slug>`, `fix/<slug>`, `chore/<slug>`) and open PRs into `dev`.
